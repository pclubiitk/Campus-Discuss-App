### This is the folder for the homepage/feed screen of the app.

On the terminal, cd into the project folder and run `yarn add moment` to install `momentJs` Javascript library for manipulating date/time.  
